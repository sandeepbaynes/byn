# byn (name reservation)

This NuGet package reserves the name **`byn`**. byn is a **Go CLI** — a
local-first secure secrets vault and credential manager — not a .NET package.
It is not a dotnet tool, so it installs no `byn` command.

## Install the real byn

```sh
go install github.com/sandeepbaynes/byn/cmd/byn@latest
brew install sandeepbaynes/tap/byn
curl -fsSL https://raw.githubusercontent.com/sandeepbaynes/byn/main/install.sh | sh
```

Homepage: https://github.com/sandeepbaynes/byn · © 2026 Sandeep Baynes · BUSL-1.1 (source-available)
