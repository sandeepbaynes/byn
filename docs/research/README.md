# Research notes — not published

Investigations and spikes: what was tried, what it cost, what it ruled out.
Kept because the reasoning is worth having later, not because it is finished.

**Nothing in this directory is published** — see `docs/design/README.md` for how
that is enforced (the site publishes only what the manifest names, and the
deploy fails if any markdown source reaches the built site).
